name: Compute Probabilities

# STUB — not yet wired up. Runs the coalitions pipeline (pool + Monte-Carlo +
# seat allocation) when new survey data lands, writing JSON to data/results/.

on:
  # push:
  #   paths: ['data/surveys/**']   # enable once script exists
  workflow_dispatch:

jobs:
  compute:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: r-lib/actions/setup-r@v2
      - name: Install dependencies
        run: Rscript -e 'install.packages(c("coalitions", "jsonlite", "dplyr", "tidyr", "purrr"))'
      - name: Compute coalition probabilities
        run: echo "TODO: Rscript scripts/compute_probabilities.R  (generalized from rlp-dashboard/compute_rlp.R)"
      # - name: Commit results
      #   run: |
      #     git config user.name "KOALA Bot"
      #     git config user.email "koala-bot@users.noreply.github.com"
      #     git add data/results/
      #     git diff --staged --quiet || git commit -m "Update results $(date -I)"
      #     git push
