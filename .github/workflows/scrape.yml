name: Scrape Polls

# STUB — not yet wired up. Scrapes latest election polls on a schedule and
# commits raw data to data/surveys/. See README "Roadmap".

on:
  # schedule:
  #   - cron: '0 */2 * * *'   # every 2 hours (enable once script exists)
  workflow_dispatch:           # manual trigger only, for now

jobs:
  scrape:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: r-lib/actions/setup-r@v2
      - name: Install dependencies
        run: Rscript -e 'install.packages(c("coalitions", "jsonlite"))'
      - name: Scrape polls
        run: echo "TODO: Rscript scripts/scrape_polls.R"
      # - name: Commit new data
      #   run: |
      #     git config user.name "KOALA Bot"
      #     git config user.email "koala-bot@users.noreply.github.com"
      #     git add data/surveys/
      #     git diff --staged --quiet || git commit -m "Update poll data $(date -I)"
      #     git push
